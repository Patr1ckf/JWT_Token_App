# JWT_Token_App
Projekt przedstawia prosty system bezpiecznego dostępu do dokumentów oparty na tokenach JWT, z backendem w Pythonie (Flask + PyJWT). Aplikacja umożliwia logowanie użytkownika, generowanie tokenu oraz weryfikację uprawnień przy pobieraniu chronionych zasobów.

Frontend uruchamiany przez: npm run dev